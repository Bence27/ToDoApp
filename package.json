{
  "devDependencies": {
    "turbo": "^1.11.3"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "node apps/backend/dist/main",
    "bs":"turbo run build && turbo run dev"
  },
  "workspaces": [
    "apps/*"
  ]

}
